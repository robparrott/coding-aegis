# Phase 5: CLI Tooling

**Status**: Not started

## Tasks

19. Catalog structure validation command
20. List packages by tier command
21. Render package for target tool command
22. Move package between tiers (promote) command

## Dependencies

- Phase 1 (Foundation) — package structure
- Phase 4 (Renderers) — render command depends on renderer implementations
